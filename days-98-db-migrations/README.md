# Day 98 — Database Migrations in CI/CD
## What We Are Studying
Automate schema changes using Flyway in GitHub Actions. Migrations run before app deployment to prevent version mismatches.
## Commands Used
| Command | Purpose |
|---------|---------|
| `flyway migrate` | Applies pending SQL migration files in order |
| `psql` | Verify schema changes in PostgreSQL |
